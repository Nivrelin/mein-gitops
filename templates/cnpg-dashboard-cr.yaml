apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: cnpg-main-dashboard
  namespace: cnpg-cluster-test
  labels:
    dashboards: grafana
spec:
  allowCrossNamespaceImport: true
  instanceSelector:
    matchLabels:
      dashboards: "grafana"
  url: https://raw.githubusercontent.com/Nivrelin/mein-gitops/main/files/cnpg-dashboard.json
---
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDatasource
metadata:
  name: cnpg-datasource
  namespace: cnpg-cluster-test
spec:
  instanceSelector:
    matchLabels:
      dashboards: "grafana"
  allowCrossNamespaceImport: true
  datasource:
    name: OpenShift-Prometheus
    type: prometheus
    access: proxy
    url: "https://thanos-querier.openshift-monitoring.svc:9091"
    isDefault: true
    jsonData:
      httpHeaderName1: 'Authorization'
      tlsSkipVerify: true
    secureJsonData:
      httpHeaderValue1: 'Bearer ${TOKEN}'
  valuesFrom:
    - target: secureJsonData.httpHeaderValue1
      secretKeyRef:
        name: grafana-sa-token
        key: token
        prefix: "Bearer " # Leerzeichen nach Bearer nicht vergessen!
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: grafana-cnpg-read-binding
  namespace: cnpg-cluster-test
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole # Case-sensitive!
  name: cluster-monitoring-view
subjects:
  - kind: ServiceAccount
    name: grafana-monitoring-sa
    namespace: monitoring-system