{{- range .Values.clusters }}
{{- if .pooler.enabled }}
apiVersion: postgresql.cnpg.io/v1
kind: Pooler
metadata:
  name: {{ .name }}-pooler
spec:
  cluster:
    name: {{ .name }}
  instances: {{ .pooler.instances | default 2 }}
  type: {{ .pooler.type | default "rw" }}
  pgbouncer:
    poolMode: {{ .pooler.poolMode | default "transaction" }}
    # Die Pool-Größe gehört in die parameters-Sektion
    parameters:
      default_pool_size: {{ .pooler.poolSize | default 20 }}
---
{{- if .pooler.monitoring }}
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: {{ .name }}-pooler-monitor
spec:
  selector:
    matchLabels:
      cnpg.io/poolerName: {{ .name }}-pooler
  podMetricsEndpoints:
  - port: metrics
    interval: 30s
{{- end }}
{{- end }}
{{- end }}